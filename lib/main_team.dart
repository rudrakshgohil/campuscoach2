import 'package:flutter/material.dart';

class main_team extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
        backgroundColor: Colors.white,
      ),backgroundColor: Colors.white,
      body: Center(
        child: Text(
          'Team Management Page',
          style: TextStyle(fontSize: 20, height: 2),
        ),
      ),
    );
  }
}
