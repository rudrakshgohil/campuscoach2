import 'package:flutter/material.dart';

class main_analysis extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
        backgroundColor: Colors.white,
      ),
      backgroundColor: Colors.white,
      body: Center(
        child: Text(
          'Analysis Page',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
